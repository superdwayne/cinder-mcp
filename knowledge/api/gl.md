---
title: "ci::gl — OpenGL Wrapper"
category: "OpenGL"
namespace: "cinder::gl"
tags: [api, reference, opengl]
---

# ci::gl — OpenGL Wrapper

High-level OpenGL abstraction layer providing managed GPU resources, shader programs, and draw calls.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### Texture2d / Texture2dRef

2D texture wrapper with format options, mipmapping, and pixel transfer.

**Key methods:**
- `create()`
- `bind()`
- `unbind()`
- `getWidth()`
- `getHeight()`
- `update()`
- `setWrap()`
- `setMinFilter()`
- `setMagFilter()`

### Fbo / FboRef

Framebuffer Object for off-screen rendering.

**Key methods:**
- `create()`
- `bindFramebuffer()`
- `unbindFramebuffer()`
- `getColorTexture()`
- `getDepthTexture()`
- `getSize()`

### GlslProg / GlslProgRef

GLSL shader program compilation, linking, and uniform management.

**Key methods:**
- `create()`
- `bind()`
- `uniform()`
- `getAttribLocation()`
- `getUniformLocation()`

### Batch / BatchRef

Combines geometry (VboMesh) with a shader (GlslProg) for efficient draw calls.

**Key methods:**
- `create()`
- `draw()`
- `replaceGlslProg()`
- `replaceVboMesh()`
- `getGlslProg()`
- `getVboMesh()`

### Vbo / VboRef

Vertex Buffer Object for raw GPU buffer management.

**Key methods:**
- `create()`
- `bind()`
- `bufferData()`
- `bufferSubData()`
- `map()`
- `unmap()`

### VboMesh / VboMeshRef

Managed vertex buffer mesh with attribute layout.

**Key methods:**
- `create()`
- `getNumVertices()`
- `getNumIndices()`
- `appendVbo()`
- `bufferAttrib()`

### Vao / VaoRef

Vertex Array Object for attribute state management.

**Key methods:**
- `create()`
- `bind()`
- `unbind()`
- `replacementBindBegin()`
- `replacementBindEnd()`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
