---
title: "ci::geom — Geometry Primitives & Modifiers"
category: "Geometry"
namespace: "cinder::geom"
tags: [api, reference, geometry]
---

# ci::geom — Geometry Primitives & Modifiers

Procedural geometry generation with composable sources and modifiers.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### Sphere

UV sphere with configurable subdivisions.

**Key methods:**
- `radius()`
- `subdivisions()`
- `center()`

### Cube

Axis-aligned cube / box.

**Key methods:**
- `size()`
- `colors()`

### Torus

Torus with major/minor radius.

**Key methods:**
- `radius()`
- `ratio()`
- `subdivisionsAxis()`
- `subdivisionsHeight()`

### Plane

2D plane in 3D space.

**Key methods:**
- `size()`
- `subdivisions()`
- `origin()`
- `normal()`

### Twist

Modifier that twists geometry along an axis.

**Key methods:**
- `axis()`
- `angle()`

### Translate / Scale / Rotate

Transform modifiers for geometry sources.

**Key methods:**
- `Translate(vec3)`
- `Scale(vec3)`
- `Rotate(float, vec3)`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
