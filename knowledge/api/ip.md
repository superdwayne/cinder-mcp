---
title: "ci::ip — Image Processing"
category: "Images"
namespace: "cinder::ip"
tags: [api, reference, images]
---

# ci::ip — Image Processing

CPU-based image processing operations on Surface and Channel types.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### Functions (free functions)

Image processing utilities operating on Surface8u / Surface32f.

**Key methods:**
- `resize()`
- `flipVertical()`
- `flipHorizontal()`
- `threshold()`
- `grayscale()`
- `blend()`
- `fill()`
- `hFlip()`
- `edgeDetectSobel()`

### Surface8u / Surface32f

CPU image buffer with 8-bit or 32-bit float channels.

**Key methods:**
- `create()`
- `getWidth()`
- `getHeight()`
- `getPixel()`
- `setPixel()`
- `getData()`
- `getRowBytes()`

### Channel8u / Channel32f

Single-channel image buffer.

**Key methods:**
- `create()`
- `getValue()`
- `setValue()`
- `getWidth()`
- `getHeight()`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
