---
title: "ci::audio — Audio Engine"
category: "Audio"
namespace: "cinder::audio"
tags: [api, reference, audio]
---

# ci::audio — Audio Engine

Node-based audio graph for synthesis, analysis, playback, and recording.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### Context

Main audio context managing the node graph and device I/O.

**Key methods:**
- `master()`
- `makeNode()`
- `enable()`
- `disable()`
- `getSampleRate()`
- `getFramesPerBlock()`

### Voice / VoiceRef

High-level audio playback for simple use cases.

**Key methods:**
- `create()`
- `start()`
- `stop()`
- `setVolume()`
- `setPan()`

### BufferPlayerNode

Plays an audio::Buffer with sample-accurate control.

**Key methods:**
- `start()`
- `stop()`
- `seek()`
- `setBuffer()`
- `isEnabled()`
- `setLoopEnabled()`

### MonitorSpectralNode

FFT analysis node providing magnitude spectrum.

**Key methods:**
- `getMagSpectrum()`
- `getNumBins()`
- `getSmoothingFactor()`
- `setFftSize()`
- `getFreqForBin()`

### GainNode

Multiplies signal amplitude.

**Key methods:**
- `setValue()`
- `getValue()`
- `getParam()`

### GenSineNode / GenOscNode

Oscillator nodes for synthesis.

**Key methods:**
- `setFreq()`
- `getFreq()`
- `start()`
- `stop()`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
