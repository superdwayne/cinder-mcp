---
title: "ci::app — Application Framework"
category: "App"
namespace: "cinder::app"
tags: [api, reference, app]
---

# ci::app — Application Framework

Application lifecycle, window management, input events, and resource loading.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### App

Base application class with setup/update/draw lifecycle.

**Key methods:**
- `setup()`
- `update()`
- `draw()`
- `cleanup()`
- `prepareSettings()`
- `getElapsedSeconds()`
- `getElapsedFrames()`
- `quit()`

### Window

Platform window abstraction.

**Key methods:**
- `getSize()`
- `setSize()`
- `getPos()`
- `setPos()`
- `setTitle()`
- `setFullScreen()`
- `isFullScreen()`
- `close()`

### MouseEvent

Mouse input event data.

**Key methods:**
- `getPos()`
- `getX()`
- `getY()`
- `isLeft()`
- `isRight()`
- `isMiddle()`
- `isShiftDown()`
- `isControlDown()`

### KeyEvent

Keyboard input event data.

**Key methods:**
- `getCode()`
- `getChar()`
- `isShiftDown()`
- `isControlDown()`
- `isAltDown()`

### FileDropEvent

File drag-and-drop event.

**Key methods:**
- `getFiles()`
- `getNumFiles()`
- `getFile()`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
