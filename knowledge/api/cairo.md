---
title: "ci::cairo — 2D Vector Graphics"
category: "2D Graphics"
namespace: "cinder::cairo"
tags: [api, reference, 2d graphics]
---

# ci::cairo — 2D Vector Graphics

Cairo-based 2D rendering with paths, gradients, text, and PDF/SVG export.

> **Note:** This is a stub generated by `index-docs.ts`. It will be enriched
> when the libcinder.org documentation scraper is implemented.

## Classes & Key Types

### Context

Cairo drawing context with path, stroke, and fill operations.

**Key methods:**
- `moveTo()`
- `lineTo()`
- `curveTo()`
- `arc()`
- `fill()`
- `stroke()`
- `setSource()`
- `setLineWidth()`
- `save()`
- `restore()`

### SurfaceImage

Pixel-backed cairo surface for rasterized output.

**Key methods:**
- `create()`
- `getSurface()`
- `getWidth()`
- `getHeight()`

### GradientLinear

Linear gradient pattern.

**Key methods:**
- `GradientLinear()`
- `addColorStop()`

### GradientRadial

Radial gradient pattern.

**Key methods:**
- `GradientRadial()`
- `addColorStop()`

## See Also

- [libcinder.org/docs](https://libcinder.org/docs) — Official API reference
- [Cinder Guide](https://libcinder.org/docs/guides/) — Tutorials and guides
